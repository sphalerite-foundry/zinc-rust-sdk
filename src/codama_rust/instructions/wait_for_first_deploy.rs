//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const WAIT_FOR_FIRST_DEPLOY_DISCRIMINATOR: [u8; 8] = [119, 118, 139, 250, 239, 205, 246, 157];

/// Accounts.
#[derive(Debug)]
pub struct WaitForFirstDeploy {
    /// Crank authorized to promote prepared rounds into the waiting state.
    pub signer: solana_address::Address,
    /// Global config containing the crank authority.
    pub config: solana_address::Address,
    /// Global board pointers for the playable waiting round and active round, if any.
    pub board: solana_address::Address,
    /// Ready round that will become the next playable waiting round.
    pub round: solana_address::Address,
    /// Secret account that must already contain the encrypted round random value.
    pub round_secret: solana_address::Address,
}

impl WaitForFirstDeploy {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.signer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.board, false));
        accounts.push(solana_instruction::AccountMeta::new(self.round, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.round_secret,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = WaitForFirstDeployInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct WaitForFirstDeployInstructionData {
    discriminator: [u8; 8],
}

impl WaitForFirstDeployInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [119, 118, 139, 250, 239, 205, 246, 157],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for WaitForFirstDeployInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `WaitForFirstDeploy`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` config
///   2. `[writable]` board
///   3. `[writable]` round
///   4. `[]` round_secret
#[derive(Clone, Debug, Default)]
pub struct WaitForFirstDeployBuilder {
    signer: Option<solana_address::Address>,
    config: Option<solana_address::Address>,
    board: Option<solana_address::Address>,
    round: Option<solana_address::Address>,
    round_secret: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl WaitForFirstDeployBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Crank authorized to promote prepared rounds into the waiting state.
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_address::Address) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// Global config containing the crank authority.
    #[inline(always)]
    pub fn config(&mut self, config: solana_address::Address) -> &mut Self {
        self.config = Some(config);
        self
    }
    /// Global board pointers for the playable waiting round and active round, if any.
    #[inline(always)]
    pub fn board(&mut self, board: solana_address::Address) -> &mut Self {
        self.board = Some(board);
        self
    }
    /// Ready round that will become the next playable waiting round.
    #[inline(always)]
    pub fn round(&mut self, round: solana_address::Address) -> &mut Self {
        self.round = Some(round);
        self
    }
    /// Secret account that must already contain the encrypted round random value.
    #[inline(always)]
    pub fn round_secret(&mut self, round_secret: solana_address::Address) -> &mut Self {
        self.round_secret = Some(round_secret);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = WaitForFirstDeploy {
            signer: self.signer.expect("signer is not set"),
            config: self.config.expect("config is not set"),
            board: self.board.expect("board is not set"),
            round: self.round.expect("round is not set"),
            round_secret: self.round_secret.expect("round_secret is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `wait_for_first_deploy` CPI accounts.
pub struct WaitForFirstDeployCpiAccounts<'a, 'b> {
    /// Crank authorized to promote prepared rounds into the waiting state.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Global config containing the crank authority.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Global board pointers for the playable waiting round and active round, if any.
    pub board: &'b solana_account_info::AccountInfo<'a>,
    /// Ready round that will become the next playable waiting round.
    pub round: &'b solana_account_info::AccountInfo<'a>,
    /// Secret account that must already contain the encrypted round random value.
    pub round_secret: &'b solana_account_info::AccountInfo<'a>,
}

/// `wait_for_first_deploy` CPI instruction.
pub struct WaitForFirstDeployCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Crank authorized to promote prepared rounds into the waiting state.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Global config containing the crank authority.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Global board pointers for the playable waiting round and active round, if any.
    pub board: &'b solana_account_info::AccountInfo<'a>,
    /// Ready round that will become the next playable waiting round.
    pub round: &'b solana_account_info::AccountInfo<'a>,
    /// Secret account that must already contain the encrypted round random value.
    pub round_secret: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> WaitForFirstDeployCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: WaitForFirstDeployCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            signer: accounts.signer,
            config: accounts.config,
            board: accounts.board,
            round: accounts.round,
            round_secret: accounts.round_secret,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.signer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.board.key, false));
        accounts.push(solana_instruction::AccountMeta::new(*self.round.key, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.round_secret.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = WaitForFirstDeployInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.signer.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.board.clone());
        account_infos.push(self.round.clone());
        account_infos.push(self.round_secret.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `WaitForFirstDeploy` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` config
///   2. `[writable]` board
///   3. `[writable]` round
///   4. `[]` round_secret
#[derive(Clone, Debug)]
pub struct WaitForFirstDeployCpiBuilder<'a, 'b> {
    instruction: Box<WaitForFirstDeployCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> WaitForFirstDeployCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(WaitForFirstDeployCpiBuilderInstruction {
            __program: program,
            signer: None,
            config: None,
            board: None,
            round: None,
            round_secret: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Crank authorized to promote prepared rounds into the waiting state.
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// Global config containing the crank authority.
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    /// Global board pointers for the playable waiting round and active round, if any.
    #[inline(always)]
    pub fn board(&mut self, board: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.board = Some(board);
        self
    }
    /// Ready round that will become the next playable waiting round.
    #[inline(always)]
    pub fn round(&mut self, round: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.round = Some(round);
        self
    }
    /// Secret account that must already contain the encrypted round random value.
    #[inline(always)]
    pub fn round_secret(
        &mut self,
        round_secret: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.round_secret = Some(round_secret);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = WaitForFirstDeployCpi {
            __program: self.instruction.__program,

            signer: self.instruction.signer.expect("signer is not set"),

            config: self.instruction.config.expect("config is not set"),

            board: self.instruction.board.expect("board is not set"),

            round: self.instruction.round.expect("round is not set"),

            round_secret: self
                .instruction
                .round_secret
                .expect("round_secret is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct WaitForFirstDeployCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    board: Option<&'b solana_account_info::AccountInfo<'a>>,
    round: Option<&'b solana_account_info::AccountInfo<'a>>,
    round_secret: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
