//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const RECOVER_SETTLE_MINER_DISCRIMINATOR: [u8; 8] = [107, 18, 214, 148, 17, 176, 106, 169];

/// Accounts.
#[derive(Debug)]
pub struct RecoverSettleMiner {
    /// Admin signer authorized by the protocol config.
    pub admin: solana_address::Address,
    /// Global config containing the admin authority and Bonanza settlement settings.
    pub config: solana_address::Address,
    /// Revealed round currently waiting on per-miner settlement.
    pub round: solana_address::Address,
    /// Optional Wildcat sidecar used by sidecar-mode rounds.
    pub round_wildcat_entries: Option<solana_address::Address>,
    /// Miner whose settlement result is recovered from admin-reviewed evidence.
    pub miner: solana_address::Address,
    /// Player profile that receives the same hidden-bonus bricks as normal settlement.
    pub player_profile: solana_address::Address,
    /// Treasury Bonanza state that may snapshot the live pot when this is the final miner.
    pub treasury: solana_address::Address,
}

impl RecoverSettleMiner {
    pub fn instruction(
        &self,
        args: RecoverSettleMinerInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: RecoverSettleMinerInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.round, false));
        if let Some(round_wildcat_entries) = self.round_wildcat_entries {
            accounts.push(solana_instruction::AccountMeta::new(
                round_wildcat_entries,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::ZINC_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new(self.miner, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.player_profile,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.treasury, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = RecoverSettleMinerInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct RecoverSettleMinerInstructionData {
    discriminator: [u8; 8],
}

impl RecoverSettleMinerInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [107, 18, 214, 148, 17, 176, 106, 169],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for RecoverSettleMinerInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct RecoverSettleMinerInstructionArgs {
    pub winning_stake: u64,
    pub selected_count: u8,
    pub reason: String,
    pub evidence_hash: [u8; 32],
}

impl RecoverSettleMinerInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `RecoverSettleMiner`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[]` config
///   2. `[writable]` round
///   3. `[writable, optional]` round_wildcat_entries
///   4. `[writable]` miner
///   5. `[writable]` player_profile
///   6. `[writable]` treasury
#[derive(Clone, Debug, Default)]
pub struct RecoverSettleMinerBuilder {
    admin: Option<solana_address::Address>,
    config: Option<solana_address::Address>,
    round: Option<solana_address::Address>,
    round_wildcat_entries: Option<solana_address::Address>,
    miner: Option<solana_address::Address>,
    player_profile: Option<solana_address::Address>,
    treasury: Option<solana_address::Address>,
    winning_stake: Option<u64>,
    selected_count: Option<u8>,
    reason: Option<String>,
    evidence_hash: Option<[u8; 32]>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl RecoverSettleMinerBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Admin signer authorized by the protocol config.
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_address::Address) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    /// Global config containing the admin authority and Bonanza settlement settings.
    #[inline(always)]
    pub fn config(&mut self, config: solana_address::Address) -> &mut Self {
        self.config = Some(config);
        self
    }
    /// Revealed round currently waiting on per-miner settlement.
    #[inline(always)]
    pub fn round(&mut self, round: solana_address::Address) -> &mut Self {
        self.round = Some(round);
        self
    }
    /// `[optional account]`
    /// Optional Wildcat sidecar used by sidecar-mode rounds.
    #[inline(always)]
    pub fn round_wildcat_entries(
        &mut self,
        round_wildcat_entries: Option<solana_address::Address>,
    ) -> &mut Self {
        self.round_wildcat_entries = round_wildcat_entries;
        self
    }
    /// Miner whose settlement result is recovered from admin-reviewed evidence.
    #[inline(always)]
    pub fn miner(&mut self, miner: solana_address::Address) -> &mut Self {
        self.miner = Some(miner);
        self
    }
    /// Player profile that receives the same hidden-bonus bricks as normal settlement.
    #[inline(always)]
    pub fn player_profile(&mut self, player_profile: solana_address::Address) -> &mut Self {
        self.player_profile = Some(player_profile);
        self
    }
    /// Treasury Bonanza state that may snapshot the live pot when this is the final miner.
    #[inline(always)]
    pub fn treasury(&mut self, treasury: solana_address::Address) -> &mut Self {
        self.treasury = Some(treasury);
        self
    }
    #[inline(always)]
    pub fn winning_stake(&mut self, winning_stake: u64) -> &mut Self {
        self.winning_stake = Some(winning_stake);
        self
    }
    #[inline(always)]
    pub fn selected_count(&mut self, selected_count: u8) -> &mut Self {
        self.selected_count = Some(selected_count);
        self
    }
    #[inline(always)]
    pub fn reason(&mut self, reason: String) -> &mut Self {
        self.reason = Some(reason);
        self
    }
    #[inline(always)]
    pub fn evidence_hash(&mut self, evidence_hash: [u8; 32]) -> &mut Self {
        self.evidence_hash = Some(evidence_hash);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = RecoverSettleMiner {
            admin: self.admin.expect("admin is not set"),
            config: self.config.expect("config is not set"),
            round: self.round.expect("round is not set"),
            round_wildcat_entries: self.round_wildcat_entries,
            miner: self.miner.expect("miner is not set"),
            player_profile: self.player_profile.expect("player_profile is not set"),
            treasury: self.treasury.expect("treasury is not set"),
        };
        let args = RecoverSettleMinerInstructionArgs {
            winning_stake: self
                .winning_stake
                .clone()
                .expect("winning_stake is not set"),
            selected_count: self
                .selected_count
                .clone()
                .expect("selected_count is not set"),
            reason: self.reason.clone().expect("reason is not set"),
            evidence_hash: self
                .evidence_hash
                .clone()
                .expect("evidence_hash is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `recover_settle_miner` CPI accounts.
pub struct RecoverSettleMinerCpiAccounts<'a, 'b> {
    /// Admin signer authorized by the protocol config.
    pub admin: &'b solana_account_info::AccountInfo<'a>,
    /// Global config containing the admin authority and Bonanza settlement settings.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Revealed round currently waiting on per-miner settlement.
    pub round: &'b solana_account_info::AccountInfo<'a>,
    /// Optional Wildcat sidecar used by sidecar-mode rounds.
    pub round_wildcat_entries: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Miner whose settlement result is recovered from admin-reviewed evidence.
    pub miner: &'b solana_account_info::AccountInfo<'a>,
    /// Player profile that receives the same hidden-bonus bricks as normal settlement.
    pub player_profile: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury Bonanza state that may snapshot the live pot when this is the final miner.
    pub treasury: &'b solana_account_info::AccountInfo<'a>,
}

/// `recover_settle_miner` CPI instruction.
pub struct RecoverSettleMinerCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Admin signer authorized by the protocol config.
    pub admin: &'b solana_account_info::AccountInfo<'a>,
    /// Global config containing the admin authority and Bonanza settlement settings.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Revealed round currently waiting on per-miner settlement.
    pub round: &'b solana_account_info::AccountInfo<'a>,
    /// Optional Wildcat sidecar used by sidecar-mode rounds.
    pub round_wildcat_entries: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Miner whose settlement result is recovered from admin-reviewed evidence.
    pub miner: &'b solana_account_info::AccountInfo<'a>,
    /// Player profile that receives the same hidden-bonus bricks as normal settlement.
    pub player_profile: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury Bonanza state that may snapshot the live pot when this is the final miner.
    pub treasury: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: RecoverSettleMinerInstructionArgs,
}

impl<'a, 'b> RecoverSettleMinerCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: RecoverSettleMinerCpiAccounts<'a, 'b>,
        args: RecoverSettleMinerInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            admin: accounts.admin,
            config: accounts.config,
            round: accounts.round,
            round_wildcat_entries: accounts.round_wildcat_entries,
            miner: accounts.miner,
            player_profile: accounts.player_profile,
            treasury: accounts.treasury,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.round.key, false));
        if let Some(round_wildcat_entries) = self.round_wildcat_entries {
            accounts.push(solana_instruction::AccountMeta::new(
                *round_wildcat_entries.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::ZINC_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new(*self.miner.key, false));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.player_profile.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.treasury.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = RecoverSettleMinerInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(8 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.round.clone());
        if let Some(round_wildcat_entries) = self.round_wildcat_entries {
            account_infos.push(round_wildcat_entries.clone());
        }
        account_infos.push(self.miner.clone());
        account_infos.push(self.player_profile.clone());
        account_infos.push(self.treasury.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `RecoverSettleMiner` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[]` config
///   2. `[writable]` round
///   3. `[writable, optional]` round_wildcat_entries
///   4. `[writable]` miner
///   5. `[writable]` player_profile
///   6. `[writable]` treasury
#[derive(Clone, Debug)]
pub struct RecoverSettleMinerCpiBuilder<'a, 'b> {
    instruction: Box<RecoverSettleMinerCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RecoverSettleMinerCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RecoverSettleMinerCpiBuilderInstruction {
            __program: program,
            admin: None,
            config: None,
            round: None,
            round_wildcat_entries: None,
            miner: None,
            player_profile: None,
            treasury: None,
            winning_stake: None,
            selected_count: None,
            reason: None,
            evidence_hash: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Admin signer authorized by the protocol config.
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    /// Global config containing the admin authority and Bonanza settlement settings.
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    /// Revealed round currently waiting on per-miner settlement.
    #[inline(always)]
    pub fn round(&mut self, round: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.round = Some(round);
        self
    }
    /// `[optional account]`
    /// Optional Wildcat sidecar used by sidecar-mode rounds.
    #[inline(always)]
    pub fn round_wildcat_entries(
        &mut self,
        round_wildcat_entries: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.round_wildcat_entries = round_wildcat_entries;
        self
    }
    /// Miner whose settlement result is recovered from admin-reviewed evidence.
    #[inline(always)]
    pub fn miner(&mut self, miner: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.miner = Some(miner);
        self
    }
    /// Player profile that receives the same hidden-bonus bricks as normal settlement.
    #[inline(always)]
    pub fn player_profile(
        &mut self,
        player_profile: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.player_profile = Some(player_profile);
        self
    }
    /// Treasury Bonanza state that may snapshot the live pot when this is the final miner.
    #[inline(always)]
    pub fn treasury(&mut self, treasury: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.treasury = Some(treasury);
        self
    }
    #[inline(always)]
    pub fn winning_stake(&mut self, winning_stake: u64) -> &mut Self {
        self.instruction.winning_stake = Some(winning_stake);
        self
    }
    #[inline(always)]
    pub fn selected_count(&mut self, selected_count: u8) -> &mut Self {
        self.instruction.selected_count = Some(selected_count);
        self
    }
    #[inline(always)]
    pub fn reason(&mut self, reason: String) -> &mut Self {
        self.instruction.reason = Some(reason);
        self
    }
    #[inline(always)]
    pub fn evidence_hash(&mut self, evidence_hash: [u8; 32]) -> &mut Self {
        self.instruction.evidence_hash = Some(evidence_hash);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = RecoverSettleMinerInstructionArgs {
            winning_stake: self
                .instruction
                .winning_stake
                .clone()
                .expect("winning_stake is not set"),
            selected_count: self
                .instruction
                .selected_count
                .clone()
                .expect("selected_count is not set"),
            reason: self.instruction.reason.clone().expect("reason is not set"),
            evidence_hash: self
                .instruction
                .evidence_hash
                .clone()
                .expect("evidence_hash is not set"),
        };
        let instruction = RecoverSettleMinerCpi {
            __program: self.instruction.__program,

            admin: self.instruction.admin.expect("admin is not set"),

            config: self.instruction.config.expect("config is not set"),

            round: self.instruction.round.expect("round is not set"),

            round_wildcat_entries: self.instruction.round_wildcat_entries,

            miner: self.instruction.miner.expect("miner is not set"),

            player_profile: self
                .instruction
                .player_profile
                .expect("player_profile is not set"),

            treasury: self.instruction.treasury.expect("treasury is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct RecoverSettleMinerCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    round: Option<&'b solana_account_info::AccountInfo<'a>>,
    round_wildcat_entries: Option<&'b solana_account_info::AccountInfo<'a>>,
    miner: Option<&'b solana_account_info::AccountInfo<'a>>,
    player_profile: Option<&'b solana_account_info::AccountInfo<'a>>,
    treasury: Option<&'b solana_account_info::AccountInfo<'a>>,
    winning_stake: Option<u64>,
    selected_count: Option<u8>,
    reason: Option<String>,
    evidence_hash: Option<[u8; 32]>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
