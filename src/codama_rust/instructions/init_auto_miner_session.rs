//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const INIT_AUTO_MINER_SESSION_DISCRIMINATOR: [u8; 8] = [242, 248, 89, 30, 189, 159, 59, 157];

/// Accounts.
#[derive(Debug)]
pub struct InitAutoMinerSession {
    /// Wallet that owns and funds the auto-miner session.
    pub authority: solana_address::Address,
    /// Player-owned auto-miner session PDA.
    pub auto_miner_session: solana_address::Address,
    /// Lifetime deploy profile for the auto-miner authority.
    pub player_profile: solana_address::Address,
    /// System program used for account creation and budget transfer.
    pub system_program: solana_address::Address,
}

impl InitAutoMinerSession {
    pub fn instruction(
        &self,
        args: InitAutoMinerSessionInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: InitAutoMinerSessionInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.authority, true));
        accounts.push(solana_instruction::AccountMeta::new(
            self.auto_miner_session,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.player_profile,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = InitAutoMinerSessionInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitAutoMinerSessionInstructionData {
    discriminator: [u8; 8],
}

impl InitAutoMinerSessionInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [242, 248, 89, 30, 189, 159, 59, 157],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for InitAutoMinerSessionInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitAutoMinerSessionInstructionArgs {
    pub executor: Address,
    pub amount_per_round: u64,
    pub mask_bits_encryption_key: [u8; 32],
    pub mask_bits_nonce: u128,
    pub mask_bits_ciphertext: [u8; 32],
    pub mask_bits_key_version: u16,
    pub initial_budget: u64,
    pub expiry_slot: Option<u64>,
    pub crank_reimbursement_lamports: u64,
    pub auto_reload_sol_rewards: bool,
}

impl InitAutoMinerSessionInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `InitAutoMinerSession`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` auto_miner_session
///   2. `[writable]` player_profile
///   3. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct InitAutoMinerSessionBuilder {
    authority: Option<solana_address::Address>,
    auto_miner_session: Option<solana_address::Address>,
    player_profile: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    executor: Option<Address>,
    amount_per_round: Option<u64>,
    mask_bits_encryption_key: Option<[u8; 32]>,
    mask_bits_nonce: Option<u128>,
    mask_bits_ciphertext: Option<[u8; 32]>,
    mask_bits_key_version: Option<u16>,
    initial_budget: Option<u64>,
    expiry_slot: Option<u64>,
    crank_reimbursement_lamports: Option<u64>,
    auto_reload_sol_rewards: Option<bool>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl InitAutoMinerSessionBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Wallet that owns and funds the auto-miner session.
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    /// Player-owned auto-miner session PDA.
    #[inline(always)]
    pub fn auto_miner_session(&mut self, auto_miner_session: solana_address::Address) -> &mut Self {
        self.auto_miner_session = Some(auto_miner_session);
        self
    }
    /// Lifetime deploy profile for the auto-miner authority.
    #[inline(always)]
    pub fn player_profile(&mut self, player_profile: solana_address::Address) -> &mut Self {
        self.player_profile = Some(player_profile);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// System program used for account creation and budget transfer.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn executor(&mut self, executor: Address) -> &mut Self {
        self.executor = Some(executor);
        self
    }
    #[inline(always)]
    pub fn amount_per_round(&mut self, amount_per_round: u64) -> &mut Self {
        self.amount_per_round = Some(amount_per_round);
        self
    }
    #[inline(always)]
    pub fn mask_bits_encryption_key(&mut self, mask_bits_encryption_key: [u8; 32]) -> &mut Self {
        self.mask_bits_encryption_key = Some(mask_bits_encryption_key);
        self
    }
    #[inline(always)]
    pub fn mask_bits_nonce(&mut self, mask_bits_nonce: u128) -> &mut Self {
        self.mask_bits_nonce = Some(mask_bits_nonce);
        self
    }
    #[inline(always)]
    pub fn mask_bits_ciphertext(&mut self, mask_bits_ciphertext: [u8; 32]) -> &mut Self {
        self.mask_bits_ciphertext = Some(mask_bits_ciphertext);
        self
    }
    #[inline(always)]
    pub fn mask_bits_key_version(&mut self, mask_bits_key_version: u16) -> &mut Self {
        self.mask_bits_key_version = Some(mask_bits_key_version);
        self
    }
    #[inline(always)]
    pub fn initial_budget(&mut self, initial_budget: u64) -> &mut Self {
        self.initial_budget = Some(initial_budget);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn expiry_slot(&mut self, expiry_slot: u64) -> &mut Self {
        self.expiry_slot = Some(expiry_slot);
        self
    }
    #[inline(always)]
    pub fn crank_reimbursement_lamports(&mut self, crank_reimbursement_lamports: u64) -> &mut Self {
        self.crank_reimbursement_lamports = Some(crank_reimbursement_lamports);
        self
    }
    #[inline(always)]
    pub fn auto_reload_sol_rewards(&mut self, auto_reload_sol_rewards: bool) -> &mut Self {
        self.auto_reload_sol_rewards = Some(auto_reload_sol_rewards);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = InitAutoMinerSession {
            authority: self.authority.expect("authority is not set"),
            auto_miner_session: self
                .auto_miner_session
                .expect("auto_miner_session is not set"),
            player_profile: self.player_profile.expect("player_profile is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = InitAutoMinerSessionInstructionArgs {
            executor: self.executor.clone().expect("executor is not set"),
            amount_per_round: self
                .amount_per_round
                .clone()
                .expect("amount_per_round is not set"),
            mask_bits_encryption_key: self
                .mask_bits_encryption_key
                .clone()
                .expect("mask_bits_encryption_key is not set"),
            mask_bits_nonce: self
                .mask_bits_nonce
                .clone()
                .expect("mask_bits_nonce is not set"),
            mask_bits_ciphertext: self
                .mask_bits_ciphertext
                .clone()
                .expect("mask_bits_ciphertext is not set"),
            mask_bits_key_version: self
                .mask_bits_key_version
                .clone()
                .expect("mask_bits_key_version is not set"),
            initial_budget: self
                .initial_budget
                .clone()
                .expect("initial_budget is not set"),
            expiry_slot: self.expiry_slot.clone(),
            crank_reimbursement_lamports: self
                .crank_reimbursement_lamports
                .clone()
                .expect("crank_reimbursement_lamports is not set"),
            auto_reload_sol_rewards: self
                .auto_reload_sol_rewards
                .clone()
                .expect("auto_reload_sol_rewards is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `init_auto_miner_session` CPI accounts.
pub struct InitAutoMinerSessionCpiAccounts<'a, 'b> {
    /// Wallet that owns and funds the auto-miner session.
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// Player-owned auto-miner session PDA.
    pub auto_miner_session: &'b solana_account_info::AccountInfo<'a>,
    /// Lifetime deploy profile for the auto-miner authority.
    pub player_profile: &'b solana_account_info::AccountInfo<'a>,
    /// System program used for account creation and budget transfer.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `init_auto_miner_session` CPI instruction.
pub struct InitAutoMinerSessionCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Wallet that owns and funds the auto-miner session.
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// Player-owned auto-miner session PDA.
    pub auto_miner_session: &'b solana_account_info::AccountInfo<'a>,
    /// Lifetime deploy profile for the auto-miner authority.
    pub player_profile: &'b solana_account_info::AccountInfo<'a>,
    /// System program used for account creation and budget transfer.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: InitAutoMinerSessionInstructionArgs,
}

impl<'a, 'b> InitAutoMinerSessionCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: InitAutoMinerSessionCpiAccounts<'a, 'b>,
        args: InitAutoMinerSessionInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            auto_miner_session: accounts.auto_miner_session,
            player_profile: accounts.player_profile,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.auto_miner_session.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.player_profile.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = InitAutoMinerSessionInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.auto_miner_session.clone());
        account_infos.push(self.player_profile.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `InitAutoMinerSession` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` auto_miner_session
///   2. `[writable]` player_profile
///   3. `[]` system_program
#[derive(Clone, Debug)]
pub struct InitAutoMinerSessionCpiBuilder<'a, 'b> {
    instruction: Box<InitAutoMinerSessionCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InitAutoMinerSessionCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(InitAutoMinerSessionCpiBuilderInstruction {
            __program: program,
            authority: None,
            auto_miner_session: None,
            player_profile: None,
            system_program: None,
            executor: None,
            amount_per_round: None,
            mask_bits_encryption_key: None,
            mask_bits_nonce: None,
            mask_bits_ciphertext: None,
            mask_bits_key_version: None,
            initial_budget: None,
            expiry_slot: None,
            crank_reimbursement_lamports: None,
            auto_reload_sol_rewards: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Wallet that owns and funds the auto-miner session.
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    /// Player-owned auto-miner session PDA.
    #[inline(always)]
    pub fn auto_miner_session(
        &mut self,
        auto_miner_session: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.auto_miner_session = Some(auto_miner_session);
        self
    }
    /// Lifetime deploy profile for the auto-miner authority.
    #[inline(always)]
    pub fn player_profile(
        &mut self,
        player_profile: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.player_profile = Some(player_profile);
        self
    }
    /// System program used for account creation and budget transfer.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn executor(&mut self, executor: Address) -> &mut Self {
        self.instruction.executor = Some(executor);
        self
    }
    #[inline(always)]
    pub fn amount_per_round(&mut self, amount_per_round: u64) -> &mut Self {
        self.instruction.amount_per_round = Some(amount_per_round);
        self
    }
    #[inline(always)]
    pub fn mask_bits_encryption_key(&mut self, mask_bits_encryption_key: [u8; 32]) -> &mut Self {
        self.instruction.mask_bits_encryption_key = Some(mask_bits_encryption_key);
        self
    }
    #[inline(always)]
    pub fn mask_bits_nonce(&mut self, mask_bits_nonce: u128) -> &mut Self {
        self.instruction.mask_bits_nonce = Some(mask_bits_nonce);
        self
    }
    #[inline(always)]
    pub fn mask_bits_ciphertext(&mut self, mask_bits_ciphertext: [u8; 32]) -> &mut Self {
        self.instruction.mask_bits_ciphertext = Some(mask_bits_ciphertext);
        self
    }
    #[inline(always)]
    pub fn mask_bits_key_version(&mut self, mask_bits_key_version: u16) -> &mut Self {
        self.instruction.mask_bits_key_version = Some(mask_bits_key_version);
        self
    }
    #[inline(always)]
    pub fn initial_budget(&mut self, initial_budget: u64) -> &mut Self {
        self.instruction.initial_budget = Some(initial_budget);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn expiry_slot(&mut self, expiry_slot: u64) -> &mut Self {
        self.instruction.expiry_slot = Some(expiry_slot);
        self
    }
    #[inline(always)]
    pub fn crank_reimbursement_lamports(&mut self, crank_reimbursement_lamports: u64) -> &mut Self {
        self.instruction.crank_reimbursement_lamports = Some(crank_reimbursement_lamports);
        self
    }
    #[inline(always)]
    pub fn auto_reload_sol_rewards(&mut self, auto_reload_sol_rewards: bool) -> &mut Self {
        self.instruction.auto_reload_sol_rewards = Some(auto_reload_sol_rewards);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = InitAutoMinerSessionInstructionArgs {
            executor: self
                .instruction
                .executor
                .clone()
                .expect("executor is not set"),
            amount_per_round: self
                .instruction
                .amount_per_round
                .clone()
                .expect("amount_per_round is not set"),
            mask_bits_encryption_key: self
                .instruction
                .mask_bits_encryption_key
                .clone()
                .expect("mask_bits_encryption_key is not set"),
            mask_bits_nonce: self
                .instruction
                .mask_bits_nonce
                .clone()
                .expect("mask_bits_nonce is not set"),
            mask_bits_ciphertext: self
                .instruction
                .mask_bits_ciphertext
                .clone()
                .expect("mask_bits_ciphertext is not set"),
            mask_bits_key_version: self
                .instruction
                .mask_bits_key_version
                .clone()
                .expect("mask_bits_key_version is not set"),
            initial_budget: self
                .instruction
                .initial_budget
                .clone()
                .expect("initial_budget is not set"),
            expiry_slot: self.instruction.expiry_slot.clone(),
            crank_reimbursement_lamports: self
                .instruction
                .crank_reimbursement_lamports
                .clone()
                .expect("crank_reimbursement_lamports is not set"),
            auto_reload_sol_rewards: self
                .instruction
                .auto_reload_sol_rewards
                .clone()
                .expect("auto_reload_sol_rewards is not set"),
        };
        let instruction = InitAutoMinerSessionCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            auto_miner_session: self
                .instruction
                .auto_miner_session
                .expect("auto_miner_session is not set"),

            player_profile: self
                .instruction
                .player_profile
                .expect("player_profile is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct InitAutoMinerSessionCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    auto_miner_session: Option<&'b solana_account_info::AccountInfo<'a>>,
    player_profile: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    executor: Option<Address>,
    amount_per_round: Option<u64>,
    mask_bits_encryption_key: Option<[u8; 32]>,
    mask_bits_nonce: Option<u128>,
    mask_bits_ciphertext: Option<[u8; 32]>,
    mask_bits_key_version: Option<u16>,
    initial_budget: Option<u64>,
    expiry_slot: Option<u64>,
    crank_reimbursement_lamports: Option<u64>,
    auto_reload_sol_rewards: Option<bool>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
