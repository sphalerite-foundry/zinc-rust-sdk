//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const REGISTER_AFFILIATE_DISCRIMINATOR: [u8; 8] = [87, 121, 99, 184, 126, 63, 103, 217];

/// Accounts.
#[derive(Debug)]
pub struct RegisterAffiliate {
    /// Player wallet binding its profile to an affiliate before deploy.
    pub signer: solana_address::Address,
    /// Lifetime profile for the signing player.
    pub player_profile: solana_address::Address,

    pub affiliate: solana_address::Address,
    /// Existing initialized player profile owned by the affiliate wallet.
    pub affiliate_profile: solana_address::Address,
    /// System program used when the signer profile must be created.
    pub system_program: solana_address::Address,
}

impl RegisterAffiliate {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.signer, true));
        accounts.push(solana_instruction::AccountMeta::new(
            self.player_profile,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.affiliate,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.affiliate_profile,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = RegisterAffiliateInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct RegisterAffiliateInstructionData {
    discriminator: [u8; 8],
}

impl RegisterAffiliateInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [87, 121, 99, 184, 126, 63, 103, 217],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for RegisterAffiliateInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `RegisterAffiliate`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[writable]` player_profile
///   2. `[]` affiliate
///   3. `[]` affiliate_profile
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct RegisterAffiliateBuilder {
    signer: Option<solana_address::Address>,
    player_profile: Option<solana_address::Address>,
    affiliate: Option<solana_address::Address>,
    affiliate_profile: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl RegisterAffiliateBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Player wallet binding its profile to an affiliate before deploy.
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_address::Address) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// Lifetime profile for the signing player.
    #[inline(always)]
    pub fn player_profile(&mut self, player_profile: solana_address::Address) -> &mut Self {
        self.player_profile = Some(player_profile);
        self
    }
    #[inline(always)]
    pub fn affiliate(&mut self, affiliate: solana_address::Address) -> &mut Self {
        self.affiliate = Some(affiliate);
        self
    }
    /// Existing initialized player profile owned by the affiliate wallet.
    #[inline(always)]
    pub fn affiliate_profile(&mut self, affiliate_profile: solana_address::Address) -> &mut Self {
        self.affiliate_profile = Some(affiliate_profile);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// System program used when the signer profile must be created.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = RegisterAffiliate {
            signer: self.signer.expect("signer is not set"),
            player_profile: self.player_profile.expect("player_profile is not set"),
            affiliate: self.affiliate.expect("affiliate is not set"),
            affiliate_profile: self
                .affiliate_profile
                .expect("affiliate_profile is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `register_affiliate` CPI accounts.
pub struct RegisterAffiliateCpiAccounts<'a, 'b> {
    /// Player wallet binding its profile to an affiliate before deploy.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Lifetime profile for the signing player.
    pub player_profile: &'b solana_account_info::AccountInfo<'a>,

    pub affiliate: &'b solana_account_info::AccountInfo<'a>,
    /// Existing initialized player profile owned by the affiliate wallet.
    pub affiliate_profile: &'b solana_account_info::AccountInfo<'a>,
    /// System program used when the signer profile must be created.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `register_affiliate` CPI instruction.
pub struct RegisterAffiliateCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Player wallet binding its profile to an affiliate before deploy.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Lifetime profile for the signing player.
    pub player_profile: &'b solana_account_info::AccountInfo<'a>,

    pub affiliate: &'b solana_account_info::AccountInfo<'a>,
    /// Existing initialized player profile owned by the affiliate wallet.
    pub affiliate_profile: &'b solana_account_info::AccountInfo<'a>,
    /// System program used when the signer profile must be created.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> RegisterAffiliateCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: RegisterAffiliateCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            signer: accounts.signer,
            player_profile: accounts.player_profile,
            affiliate: accounts.affiliate,
            affiliate_profile: accounts.affiliate_profile,
            system_program: accounts.system_program,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.signer.key, true));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.player_profile.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.affiliate.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.affiliate_profile.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = RegisterAffiliateInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.signer.clone());
        account_infos.push(self.player_profile.clone());
        account_infos.push(self.affiliate.clone());
        account_infos.push(self.affiliate_profile.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `RegisterAffiliate` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[writable]` player_profile
///   2. `[]` affiliate
///   3. `[]` affiliate_profile
///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct RegisterAffiliateCpiBuilder<'a, 'b> {
    instruction: Box<RegisterAffiliateCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RegisterAffiliateCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RegisterAffiliateCpiBuilderInstruction {
            __program: program,
            signer: None,
            player_profile: None,
            affiliate: None,
            affiliate_profile: None,
            system_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Player wallet binding its profile to an affiliate before deploy.
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// Lifetime profile for the signing player.
    #[inline(always)]
    pub fn player_profile(
        &mut self,
        player_profile: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.player_profile = Some(player_profile);
        self
    }
    #[inline(always)]
    pub fn affiliate(&mut self, affiliate: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.affiliate = Some(affiliate);
        self
    }
    /// Existing initialized player profile owned by the affiliate wallet.
    #[inline(always)]
    pub fn affiliate_profile(
        &mut self,
        affiliate_profile: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.affiliate_profile = Some(affiliate_profile);
        self
    }
    /// System program used when the signer profile must be created.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = RegisterAffiliateCpi {
            __program: self.instruction.__program,

            signer: self.instruction.signer.expect("signer is not set"),

            player_profile: self
                .instruction
                .player_profile
                .expect("player_profile is not set"),

            affiliate: self.instruction.affiliate.expect("affiliate is not set"),

            affiliate_profile: self
                .instruction
                .affiliate_profile
                .expect("affiliate_profile is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct RegisterAffiliateCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    player_profile: Option<&'b solana_account_info::AccountInfo<'a>>,
    affiliate: Option<&'b solana_account_info::AccountInfo<'a>>,
    affiliate_profile: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
