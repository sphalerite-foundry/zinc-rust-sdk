//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const WRAP_BUYBACK_SOL_DISCRIMINATOR: [u8; 8] = [230, 81, 102, 81, 194, 180, 24, 22];

/// Accounts.
#[derive(Debug)]
pub struct WrapBuybackSol {
    /// Crank authority that pays ATA rent when needed and requests the wrap.
    pub signer: solana_address::Address,
    /// Global config containing the crank authority and treasury address.
    pub config: solana_address::Address,
    /// Treasury PDA that owns protocol token custody accounts.
    pub treasury: solana_address::Address,
    /// Program-owned lamport vault that accumulates deploy-time buyback SOL.
    pub buyback_sol_vault: solana_address::Address,
    /// SPL native mint used for treasury-owned wrapped SOL.
    pub wsol_mint: solana_address::Address,
    /// Treasury-owned WSOL account that receives wrapped buyback SOL.
    pub treasury_wsol_token_account: solana_address::Address,
    /// Associated Token Program used to create the treasury WSOL ATA.
    pub associated_token_program: solana_address::Address,
    /// SPL Token Program that owns the WSOL account.
    pub token_program: solana_address::Address,
    /// System Program used for ATA creation when the WSOL account is missing.
    pub system_program: solana_address::Address,
}

impl WrapBuybackSol {
    pub fn instruction(
        &self,
        args: WrapBuybackSolInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: WrapBuybackSolInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(9 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.signer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.treasury, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.buyback_sol_vault,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.wsol_mint,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.treasury_wsol_token_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.associated_token_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.token_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = WrapBuybackSolInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct WrapBuybackSolInstructionData {
    discriminator: [u8; 8],
}

impl WrapBuybackSolInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [230, 81, 102, 81, 194, 180, 24, 22],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for WrapBuybackSolInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct WrapBuybackSolInstructionArgs {
    pub amount: u64,
}

impl WrapBuybackSolInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `WrapBuybackSol`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` config
///   2. `[writable]` treasury
///   3. `[writable]` buyback_sol_vault
///   4. `[optional]` wsol_mint (default to `So11111111111111111111111111111111111111112`)
///   5. `[writable]` treasury_wsol_token_account
///   6. `[optional]` associated_token_program (default to `ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL`)
///   7. `[optional]` token_program (default to `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA`)
///   8. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct WrapBuybackSolBuilder {
    signer: Option<solana_address::Address>,
    config: Option<solana_address::Address>,
    treasury: Option<solana_address::Address>,
    buyback_sol_vault: Option<solana_address::Address>,
    wsol_mint: Option<solana_address::Address>,
    treasury_wsol_token_account: Option<solana_address::Address>,
    associated_token_program: Option<solana_address::Address>,
    token_program: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    amount: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl WrapBuybackSolBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Crank authority that pays ATA rent when needed and requests the wrap.
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_address::Address) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// Global config containing the crank authority and treasury address.
    #[inline(always)]
    pub fn config(&mut self, config: solana_address::Address) -> &mut Self {
        self.config = Some(config);
        self
    }
    /// Treasury PDA that owns protocol token custody accounts.
    #[inline(always)]
    pub fn treasury(&mut self, treasury: solana_address::Address) -> &mut Self {
        self.treasury = Some(treasury);
        self
    }
    /// Program-owned lamport vault that accumulates deploy-time buyback SOL.
    #[inline(always)]
    pub fn buyback_sol_vault(&mut self, buyback_sol_vault: solana_address::Address) -> &mut Self {
        self.buyback_sol_vault = Some(buyback_sol_vault);
        self
    }
    /// `[optional account, default to 'So11111111111111111111111111111111111111112']`
    /// SPL native mint used for treasury-owned wrapped SOL.
    #[inline(always)]
    pub fn wsol_mint(&mut self, wsol_mint: solana_address::Address) -> &mut Self {
        self.wsol_mint = Some(wsol_mint);
        self
    }
    /// Treasury-owned WSOL account that receives wrapped buyback SOL.
    #[inline(always)]
    pub fn treasury_wsol_token_account(
        &mut self,
        treasury_wsol_token_account: solana_address::Address,
    ) -> &mut Self {
        self.treasury_wsol_token_account = Some(treasury_wsol_token_account);
        self
    }
    /// `[optional account, default to 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL']`
    /// Associated Token Program used to create the treasury WSOL ATA.
    #[inline(always)]
    pub fn associated_token_program(
        &mut self,
        associated_token_program: solana_address::Address,
    ) -> &mut Self {
        self.associated_token_program = Some(associated_token_program);
        self
    }
    /// `[optional account, default to 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA']`
    /// SPL Token Program that owns the WSOL account.
    #[inline(always)]
    pub fn token_program(&mut self, token_program: solana_address::Address) -> &mut Self {
        self.token_program = Some(token_program);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// System Program used for ATA creation when the WSOL account is missing.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.amount = Some(amount);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = WrapBuybackSol {
            signer: self.signer.expect("signer is not set"),
            config: self.config.expect("config is not set"),
            treasury: self.treasury.expect("treasury is not set"),
            buyback_sol_vault: self
                .buyback_sol_vault
                .expect("buyback_sol_vault is not set"),
            wsol_mint: self.wsol_mint.unwrap_or(solana_address::address!(
                "So11111111111111111111111111111111111111112"
            )),
            treasury_wsol_token_account: self
                .treasury_wsol_token_account
                .expect("treasury_wsol_token_account is not set"),
            associated_token_program: self.associated_token_program.unwrap_or(
                solana_address::address!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
            ),
            token_program: self.token_program.unwrap_or(solana_address::address!(
                "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"
            )),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = WrapBuybackSolInstructionArgs {
            amount: self.amount.clone().expect("amount is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `wrap_buyback_sol` CPI accounts.
pub struct WrapBuybackSolCpiAccounts<'a, 'b> {
    /// Crank authority that pays ATA rent when needed and requests the wrap.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Global config containing the crank authority and treasury address.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury PDA that owns protocol token custody accounts.
    pub treasury: &'b solana_account_info::AccountInfo<'a>,
    /// Program-owned lamport vault that accumulates deploy-time buyback SOL.
    pub buyback_sol_vault: &'b solana_account_info::AccountInfo<'a>,
    /// SPL native mint used for treasury-owned wrapped SOL.
    pub wsol_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury-owned WSOL account that receives wrapped buyback SOL.
    pub treasury_wsol_token_account: &'b solana_account_info::AccountInfo<'a>,
    /// Associated Token Program used to create the treasury WSOL ATA.
    pub associated_token_program: &'b solana_account_info::AccountInfo<'a>,
    /// SPL Token Program that owns the WSOL account.
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
    /// System Program used for ATA creation when the WSOL account is missing.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `wrap_buyback_sol` CPI instruction.
pub struct WrapBuybackSolCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Crank authority that pays ATA rent when needed and requests the wrap.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Global config containing the crank authority and treasury address.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury PDA that owns protocol token custody accounts.
    pub treasury: &'b solana_account_info::AccountInfo<'a>,
    /// Program-owned lamport vault that accumulates deploy-time buyback SOL.
    pub buyback_sol_vault: &'b solana_account_info::AccountInfo<'a>,
    /// SPL native mint used for treasury-owned wrapped SOL.
    pub wsol_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury-owned WSOL account that receives wrapped buyback SOL.
    pub treasury_wsol_token_account: &'b solana_account_info::AccountInfo<'a>,
    /// Associated Token Program used to create the treasury WSOL ATA.
    pub associated_token_program: &'b solana_account_info::AccountInfo<'a>,
    /// SPL Token Program that owns the WSOL account.
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
    /// System Program used for ATA creation when the WSOL account is missing.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: WrapBuybackSolInstructionArgs,
}

impl<'a, 'b> WrapBuybackSolCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: WrapBuybackSolCpiAccounts<'a, 'b>,
        args: WrapBuybackSolInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            signer: accounts.signer,
            config: accounts.config,
            treasury: accounts.treasury,
            buyback_sol_vault: accounts.buyback_sol_vault,
            wsol_mint: accounts.wsol_mint,
            treasury_wsol_token_account: accounts.treasury_wsol_token_account,
            associated_token_program: accounts.associated_token_program,
            token_program: accounts.token_program,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(9 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.signer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.treasury.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.buyback_sol_vault.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.wsol_mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.treasury_wsol_token_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.associated_token_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.token_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = WrapBuybackSolInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(10 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.signer.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.treasury.clone());
        account_infos.push(self.buyback_sol_vault.clone());
        account_infos.push(self.wsol_mint.clone());
        account_infos.push(self.treasury_wsol_token_account.clone());
        account_infos.push(self.associated_token_program.clone());
        account_infos.push(self.token_program.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `WrapBuybackSol` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` config
///   2. `[writable]` treasury
///   3. `[writable]` buyback_sol_vault
///   4. `[]` wsol_mint
///   5. `[writable]` treasury_wsol_token_account
///   6. `[]` associated_token_program
///   7. `[]` token_program
///   8. `[]` system_program
#[derive(Clone, Debug)]
pub struct WrapBuybackSolCpiBuilder<'a, 'b> {
    instruction: Box<WrapBuybackSolCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> WrapBuybackSolCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(WrapBuybackSolCpiBuilderInstruction {
            __program: program,
            signer: None,
            config: None,
            treasury: None,
            buyback_sol_vault: None,
            wsol_mint: None,
            treasury_wsol_token_account: None,
            associated_token_program: None,
            token_program: None,
            system_program: None,
            amount: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Crank authority that pays ATA rent when needed and requests the wrap.
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// Global config containing the crank authority and treasury address.
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    /// Treasury PDA that owns protocol token custody accounts.
    #[inline(always)]
    pub fn treasury(&mut self, treasury: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.treasury = Some(treasury);
        self
    }
    /// Program-owned lamport vault that accumulates deploy-time buyback SOL.
    #[inline(always)]
    pub fn buyback_sol_vault(
        &mut self,
        buyback_sol_vault: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.buyback_sol_vault = Some(buyback_sol_vault);
        self
    }
    /// SPL native mint used for treasury-owned wrapped SOL.
    #[inline(always)]
    pub fn wsol_mint(&mut self, wsol_mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.wsol_mint = Some(wsol_mint);
        self
    }
    /// Treasury-owned WSOL account that receives wrapped buyback SOL.
    #[inline(always)]
    pub fn treasury_wsol_token_account(
        &mut self,
        treasury_wsol_token_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.treasury_wsol_token_account = Some(treasury_wsol_token_account);
        self
    }
    /// Associated Token Program used to create the treasury WSOL ATA.
    #[inline(always)]
    pub fn associated_token_program(
        &mut self,
        associated_token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.associated_token_program = Some(associated_token_program);
        self
    }
    /// SPL Token Program that owns the WSOL account.
    #[inline(always)]
    pub fn token_program(
        &mut self,
        token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.token_program = Some(token_program);
        self
    }
    /// System Program used for ATA creation when the WSOL account is missing.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.instruction.amount = Some(amount);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = WrapBuybackSolInstructionArgs {
            amount: self.instruction.amount.clone().expect("amount is not set"),
        };
        let instruction = WrapBuybackSolCpi {
            __program: self.instruction.__program,

            signer: self.instruction.signer.expect("signer is not set"),

            config: self.instruction.config.expect("config is not set"),

            treasury: self.instruction.treasury.expect("treasury is not set"),

            buyback_sol_vault: self
                .instruction
                .buyback_sol_vault
                .expect("buyback_sol_vault is not set"),

            wsol_mint: self.instruction.wsol_mint.expect("wsol_mint is not set"),

            treasury_wsol_token_account: self
                .instruction
                .treasury_wsol_token_account
                .expect("treasury_wsol_token_account is not set"),

            associated_token_program: self
                .instruction
                .associated_token_program
                .expect("associated_token_program is not set"),

            token_program: self
                .instruction
                .token_program
                .expect("token_program is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct WrapBuybackSolCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    treasury: Option<&'b solana_account_info::AccountInfo<'a>>,
    buyback_sol_vault: Option<&'b solana_account_info::AccountInfo<'a>>,
    wsol_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    treasury_wsol_token_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    associated_token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    amount: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
