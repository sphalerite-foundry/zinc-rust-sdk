//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::codama_rust::types::ZincMintMetadataArgs;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const UPDATE_ZINC_MINT_METADATA_DISCRIMINATOR: [u8; 8] = [129, 29, 107, 197, 70, 193, 189, 186];

/// Accounts.
#[derive(Debug)]
pub struct UpdateZincMintMetadata {
    /// Admin authorized to update protocol ZINC mint metadata.
    pub admin: solana_address::Address,
    /// Global config that stores the protocol admin address.
    pub config: solana_address::Address,
    /// Treasury PDA that owns the ZINC mint metadata update authority.
    pub treasury: solana_address::Address,
    /// Protocol ZINC mint whose metadata is being updated.
    pub zinc_mint: solana_address::Address,
    /// Metaplex metadata account attached to the protocol ZINC mint.
    pub zinc_metadata: solana_address::Address,
    /// Metaplex Token Metadata Program used to update ZINC mint metadata.
    pub metadata_program: solana_address::Address,
}

impl UpdateZincMintMetadata {
    pub fn instruction(
        &self,
        args: UpdateZincMintMetadataInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpdateZincMintMetadataInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.treasury,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.zinc_mint,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.zinc_metadata,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.metadata_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UpdateZincMintMetadataInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateZincMintMetadataInstructionData {
    discriminator: [u8; 8],
}

impl UpdateZincMintMetadataInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [129, 29, 107, 197, 70, 193, 189, 186],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UpdateZincMintMetadataInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateZincMintMetadataInstructionArgs {
    pub args: ZincMintMetadataArgs,
}

impl UpdateZincMintMetadataInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UpdateZincMintMetadata`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[]` config
///   2. `[]` treasury
///   3. `[]` zinc_mint
///   4. `[writable]` zinc_metadata
///   5. `[optional]` metadata_program (default to `metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s`)
#[derive(Clone, Debug, Default)]
pub struct UpdateZincMintMetadataBuilder {
    admin: Option<solana_address::Address>,
    config: Option<solana_address::Address>,
    treasury: Option<solana_address::Address>,
    zinc_mint: Option<solana_address::Address>,
    zinc_metadata: Option<solana_address::Address>,
    metadata_program: Option<solana_address::Address>,
    args: Option<ZincMintMetadataArgs>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateZincMintMetadataBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Admin authorized to update protocol ZINC mint metadata.
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_address::Address) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    /// Global config that stores the protocol admin address.
    #[inline(always)]
    pub fn config(&mut self, config: solana_address::Address) -> &mut Self {
        self.config = Some(config);
        self
    }
    /// Treasury PDA that owns the ZINC mint metadata update authority.
    #[inline(always)]
    pub fn treasury(&mut self, treasury: solana_address::Address) -> &mut Self {
        self.treasury = Some(treasury);
        self
    }
    /// Protocol ZINC mint whose metadata is being updated.
    #[inline(always)]
    pub fn zinc_mint(&mut self, zinc_mint: solana_address::Address) -> &mut Self {
        self.zinc_mint = Some(zinc_mint);
        self
    }
    /// Metaplex metadata account attached to the protocol ZINC mint.
    #[inline(always)]
    pub fn zinc_metadata(&mut self, zinc_metadata: solana_address::Address) -> &mut Self {
        self.zinc_metadata = Some(zinc_metadata);
        self
    }
    /// `[optional account, default to 'metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s']`
    /// Metaplex Token Metadata Program used to update ZINC mint metadata.
    #[inline(always)]
    pub fn metadata_program(&mut self, metadata_program: solana_address::Address) -> &mut Self {
        self.metadata_program = Some(metadata_program);
        self
    }
    #[inline(always)]
    pub fn args(&mut self, args: ZincMintMetadataArgs) -> &mut Self {
        self.args = Some(args);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpdateZincMintMetadata {
            admin: self.admin.expect("admin is not set"),
            config: self.config.expect("config is not set"),
            treasury: self.treasury.expect("treasury is not set"),
            zinc_mint: self.zinc_mint.expect("zinc_mint is not set"),
            zinc_metadata: self.zinc_metadata.expect("zinc_metadata is not set"),
            metadata_program: self.metadata_program.unwrap_or(solana_address::address!(
                "metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s"
            )),
        };
        let args = UpdateZincMintMetadataInstructionArgs {
            args: self.args.clone().expect("args is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `update_zinc_mint_metadata` CPI accounts.
pub struct UpdateZincMintMetadataCpiAccounts<'a, 'b> {
    /// Admin authorized to update protocol ZINC mint metadata.
    pub admin: &'b solana_account_info::AccountInfo<'a>,
    /// Global config that stores the protocol admin address.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury PDA that owns the ZINC mint metadata update authority.
    pub treasury: &'b solana_account_info::AccountInfo<'a>,
    /// Protocol ZINC mint whose metadata is being updated.
    pub zinc_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Metaplex metadata account attached to the protocol ZINC mint.
    pub zinc_metadata: &'b solana_account_info::AccountInfo<'a>,
    /// Metaplex Token Metadata Program used to update ZINC mint metadata.
    pub metadata_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `update_zinc_mint_metadata` CPI instruction.
pub struct UpdateZincMintMetadataCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Admin authorized to update protocol ZINC mint metadata.
    pub admin: &'b solana_account_info::AccountInfo<'a>,
    /// Global config that stores the protocol admin address.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Treasury PDA that owns the ZINC mint metadata update authority.
    pub treasury: &'b solana_account_info::AccountInfo<'a>,
    /// Protocol ZINC mint whose metadata is being updated.
    pub zinc_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Metaplex metadata account attached to the protocol ZINC mint.
    pub zinc_metadata: &'b solana_account_info::AccountInfo<'a>,
    /// Metaplex Token Metadata Program used to update ZINC mint metadata.
    pub metadata_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: UpdateZincMintMetadataInstructionArgs,
}

impl<'a, 'b> UpdateZincMintMetadataCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpdateZincMintMetadataCpiAccounts<'a, 'b>,
        args: UpdateZincMintMetadataInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            admin: accounts.admin,
            config: accounts.config,
            treasury: accounts.treasury,
            zinc_mint: accounts.zinc_mint,
            zinc_metadata: accounts.zinc_metadata,
            metadata_program: accounts.metadata_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.treasury.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.zinc_mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.zinc_metadata.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.metadata_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = UpdateZincMintMetadataInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::ZINC_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(7 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.treasury.clone());
        account_infos.push(self.zinc_mint.clone());
        account_infos.push(self.zinc_metadata.clone());
        account_infos.push(self.metadata_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpdateZincMintMetadata` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[]` config
///   2. `[]` treasury
///   3. `[]` zinc_mint
///   4. `[writable]` zinc_metadata
///   5. `[]` metadata_program
#[derive(Clone, Debug)]
pub struct UpdateZincMintMetadataCpiBuilder<'a, 'b> {
    instruction: Box<UpdateZincMintMetadataCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateZincMintMetadataCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpdateZincMintMetadataCpiBuilderInstruction {
            __program: program,
            admin: None,
            config: None,
            treasury: None,
            zinc_mint: None,
            zinc_metadata: None,
            metadata_program: None,
            args: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Admin authorized to update protocol ZINC mint metadata.
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    /// Global config that stores the protocol admin address.
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    /// Treasury PDA that owns the ZINC mint metadata update authority.
    #[inline(always)]
    pub fn treasury(&mut self, treasury: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.treasury = Some(treasury);
        self
    }
    /// Protocol ZINC mint whose metadata is being updated.
    #[inline(always)]
    pub fn zinc_mint(&mut self, zinc_mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.zinc_mint = Some(zinc_mint);
        self
    }
    /// Metaplex metadata account attached to the protocol ZINC mint.
    #[inline(always)]
    pub fn zinc_metadata(
        &mut self,
        zinc_metadata: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.zinc_metadata = Some(zinc_metadata);
        self
    }
    /// Metaplex Token Metadata Program used to update ZINC mint metadata.
    #[inline(always)]
    pub fn metadata_program(
        &mut self,
        metadata_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.metadata_program = Some(metadata_program);
        self
    }
    #[inline(always)]
    pub fn args(&mut self, args: ZincMintMetadataArgs) -> &mut Self {
        self.instruction.args = Some(args);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpdateZincMintMetadataInstructionArgs {
            args: self.instruction.args.clone().expect("args is not set"),
        };
        let instruction = UpdateZincMintMetadataCpi {
            __program: self.instruction.__program,

            admin: self.instruction.admin.expect("admin is not set"),

            config: self.instruction.config.expect("config is not set"),

            treasury: self.instruction.treasury.expect("treasury is not set"),

            zinc_mint: self.instruction.zinc_mint.expect("zinc_mint is not set"),

            zinc_metadata: self
                .instruction
                .zinc_metadata
                .expect("zinc_metadata is not set"),

            metadata_program: self
                .instruction
                .metadata_program
                .expect("metadata_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpdateZincMintMetadataCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    treasury: Option<&'b solana_account_info::AccountInfo<'a>>,
    zinc_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    zinc_metadata: Option<&'b solana_account_info::AccountInfo<'a>>,
    metadata_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    args: Option<ZincMintMetadataArgs>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
