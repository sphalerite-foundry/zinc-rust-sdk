//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct AutoMinerSession {
    pub discriminator: [u8; 8],
    /// Wallet that owns and controls this session.
    pub authority: Address,
    /// Crank wallet allowed to execute auto deploys.
    pub executor: Address,
    /// Gross lamports to deploy into each eligible round.
    pub amount_per_round: u64,
    /// Ephemeral public key used to encrypt the tile-pattern bits to the crank.
    pub mask_bits_encryption_key: [u8; 32],
    /// Rescue CTR nonce used for the encrypted tile-pattern bits.
    pub mask_bits_nonce: u128,
    /// Encrypted tile-pattern bits readable only by the configured crank.
    pub mask_bits_ciphertext: [u8; 32],
    /// Crank encryption-key version used by the browser when encrypting the pattern.
    pub mask_bits_key_version: u16,
    /// User-funded lamport budget available for deploys and reimbursements.
    pub remaining_budget: u64,
    /// Number of successful deploys already executed.
    pub rounds_used: u64,
    /// Whether the authority paused auto execution.
    pub paused: bool,
    /// Fixed lamport reimbursement paid to the crank after a successful deploy.
    pub crank_reimbursement_lamports: u64,
    /// PDA bump seed.
    pub bump: u8,
    /// Optional last slot at which the session can still deploy.
    pub expiry_slot: Option<u64>,
}

pub const AUTO_MINER_SESSION_DISCRIMINATOR: [u8; 8] = [9, 91, 249, 138, 188, 84, 49, 61];

impl AutoMinerSession {
    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for AutoMinerSession {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_auto_miner_session(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<AutoMinerSession>, std::io::Error> {
    let accounts = fetch_all_auto_miner_session(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_auto_miner_session(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<AutoMinerSession>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<AutoMinerSession>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        let data = AutoMinerSession::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_auto_miner_session(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<AutoMinerSession>, std::io::Error> {
    let accounts = fetch_all_maybe_auto_miner_session(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_auto_miner_session(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<AutoMinerSession>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<AutoMinerSession>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = AutoMinerSession::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for AutoMinerSession {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for AutoMinerSession {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for AutoMinerSession {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::ZINC_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for AutoMinerSession {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for AutoMinerSession {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
