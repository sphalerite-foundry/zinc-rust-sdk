//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct BuybackPool {
    pub discriminator: [u8; 8],
    /// PDA bump used to re-derive this account.
    pub bump: u8,
    /// Meteora DAMM v2 program that owns the pool.
    pub meteora_program: Address,
    /// Meteora static pool config used to initialize the pool.
    pub damm_config: Address,
    /// Meteora pool authority PDA.
    pub pool_authority: Address,
    /// Meteora pool account used for buybacks.
    pub pool: Address,
    /// Initial liquidity position account for the pool.
    pub position: Address,
    /// Token-2022 NFT mint representing the initial position.
    pub position_nft_mint: Address,
    /// Token-2022 account holding the initial position NFT.
    pub position_nft_account: Address,
    /// Pool token A mint.
    pub token_a_mint: Address,
    /// Pool token B mint.
    pub token_b_mint: Address,
    /// Pool token A vault.
    pub token_a_vault: Address,
    /// Pool token B vault.
    pub token_b_vault: Address,
    /// SPL token program for token A.
    pub token_a_program: Address,
    /// SPL token program for token B.
    pub token_b_program: Address,
    /// Meteora event authority PDA required by event CPI instructions.
    pub event_authority: Address,
    /// Whether the pool's token A side is WSOL.
    pub wsol_is_token_a: bool,
    /// Initial WSOL amount deposited into the pool.
    pub initial_wsol_lamports: u64,
    /// Initial ZINC amount deposited into the pool.
    pub initial_zinc_amount: u64,
    /// Initial Meteora liquidity parameter used during pool creation.
    pub liquidity: u128,
    /// Initial sqrt price parameter used during pool creation.
    pub sqrt_price: u128,
    /// Optional activation point used during pool creation.
    pub activation_point: Option<u64>,
}

pub const BUYBACK_POOL_DISCRIMINATOR: [u8; 8] = [30, 9, 48, 171, 48, 116, 22, 165];

impl BuybackPool {
    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for BuybackPool {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_buyback_pool(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<BuybackPool>, std::io::Error> {
    let accounts = fetch_all_buyback_pool(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_buyback_pool(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<BuybackPool>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<BuybackPool>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        let data = BuybackPool::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_buyback_pool(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<BuybackPool>, std::io::Error> {
    let accounts = fetch_all_maybe_buyback_pool(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_buyback_pool(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<BuybackPool>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<BuybackPool>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = BuybackPool::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for BuybackPool {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for BuybackPool {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for BuybackPool {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::ZINC_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for BuybackPool {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for BuybackPool {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
